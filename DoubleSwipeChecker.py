##import Datetime function
import datetime

x = datetime.datetime.now()

Day/TimeFunction()
 ## variables per day to make the read reuseable
 days = 1:31
 membername = ""
 strikes
 ## read the date
 import csv
 
 with open("octoberusage.csv", "r") as file:
  date = csv.reader(file)
 while date < 31:
 ## filter by day of month
  while day == day:
      if day == day:
       day = daycheck
        pass
      elif day =/= day
      day += 1
        return
        
NameFunction()
 ## read name from the date
  membername
 ## does this name appear again?
  if membername = membername 
 ## if yes pass it to the calculator
 ## if no leave it
  elif
      return
      
def CalculationFunction()
 ## is the difference greater than 3 hours?
  if x - y > 180
 ## if yes remove it and count a strike
 
 ## if no put it down
  else
    return
